AdvPipe_WithTransition="Enables you to control the resulting surfaces at each sharp point:<br>If the check box is selected, the resulting surfaces will be positionally continuous (G0).<br>If it is cleared the resulting surfaces will not be continuous."
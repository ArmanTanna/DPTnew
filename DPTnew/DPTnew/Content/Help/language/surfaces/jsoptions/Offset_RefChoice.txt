Offset_RefChoice="Choose where to create the offset surfaces:<br><br>Normal -- creates the offset surfaces along the normal to<br>each of the surfaces (either on the positive or negative<br>side: you can double-click the handle to invert the orientation).<br><br>View -- creates the offset surfaces on the outside part of the<br>surface relative to the current view, no matter how the normals<br>are oriented."
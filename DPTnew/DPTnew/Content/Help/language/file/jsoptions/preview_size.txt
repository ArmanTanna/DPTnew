preview_size="Graphics User Interface object size (x, y). Each configuration has its own size, since it is used inside different Graphics User Interface objects. When a preview image is selected in thinkdesign, the one with the closest size to the Graphics User Interface object including it is chosen."
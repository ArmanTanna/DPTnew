StretchScale_Keepgroups="When the Inside group check box (the one of the transform command) is selected, the selected entities are copied inside the groups. When the Inside group box (the one of the transform command) is cleared, the selected entities are copied outside the groups. This option works only when group (open or closed) and the Group check box in the Inside area is selected from Selection Filter dialog box (using SelectFilter ... from the context menu)."
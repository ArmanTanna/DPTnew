@{
    ViewBag.Title = "Internos";
}
@section featured {
    <section class="featured">
        <div class="content-wrapper">
            <hgroup class="title">
                <h1>@ViewBag.Title</h1>
            </hgroup>
            <p>
                Dptcorporate intranet site
            </p>
        </div>
    </section>
}

<table style="width:100%">
    <tbody>
        <tr>
            <th style="width:33%">
                @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin") || Roles.IsUserInRole(WebSecurity.CurrentUserName, "Internal"))
                {
                    <a href="~/Activity" target="_blank"><u>Activities</u></a>
                }
            </th>
            <th style="width:33%">
                @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin"))
                {
                    <u>
                        @Html.ActionLink("Safenet Sync", "SyncDB", "Company", null, new { onclick = "return confirm('Would you like to continue?')" })
                    </u>
                }
            </th>
            <th style="width:33%">
                @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin"))
                {
                    <u>
                        @Html.ActionLink("Update Product List", "UpdateProductList", "Company", null, new { onclick = "return confirm('Would you like to continue?')" })
                    </u>
                }
            </th>
        </tr>
        <tr>
            <td>
                @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin") || Roles.IsUserInRole(WebSecurity.CurrentUserName, "Internal"))
                {
                    <text> Timesheet</text>
                }
            </td>
            <td>
                @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin"))
                {
                    <text> Synchronize the site company info's with Safenet.</text>
                }
            </td>
            <td>
                @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin"))
                {
                    <text> Synchronize the Safenet product list info's with the site.</text>
                }
            </td>
        </tr>
    </tbody>
</table>
<table style="width:100%">
    <tbody>
        <tr>
            <th style="width:33%">
                @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin"))
                {
                    <u>
                        @Html.ActionLink("MailChimp Sync", "UpdateMailStatus", "Internos", null, new { onclick = "return confirm('Would you like to continue?')" })
                    </u>
                }
            </th>
            <th style="width:33%">
                @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin"))
                {
                    <u>
                        @Html.ActionLink("Var Companies Chart", "VarCompanies", "Internos", null, null)
                    </u>
                }
            </th>
            <th style="width:33%">
                @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin"))
                {
                    <u>
                        @Html.ActionLink("Retrive Geolocalization from IP", "UnknownCompanies", "Internos", null, new { onclick = "return confirm('Would you like to continue?')" })
                    </u>
                }
            </th>
        </tr>
        <tr>
            <td>
                @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin"))
                {
                    <text> Synchronize the mailchimp English, Italian and Japanese unsubscriber & cleaned newsletter list info's with the site.</text>
                }
            </td>
            <td>
                @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin"))
                {
                    <text> Chart test: how many companies are owned by each VAR.</text>
                }
            </td>
            <td>
                @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin"))
                {
                    <text> For unknown customer name of the machine ids retrieve the geolocalization information from IP.</text>
                }
            </td>
        </tr>
    </tbody>
</table>
<table style="width:100%">
    <tbody>
        <tr>
            <th style="width:33%">
                @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin"))
                {
                    <u>
                        @Html.ActionLink("Zero Initiative email", "SendZeroCampaignMail", "Internos", null, new { onclick = "return confirm('Would you like to continue?')" })
                    </u>
                }
            </th>
            <th style="width:33%">
                @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin"))
                {
                    <u>
                        @Html.ActionLink("Zero Bis Initiative email", "SendZeroBisCampaignMail", "Internos", null, new { onclick = "return confirm('Would you like to continue?')" })
                    </u>
                }
            </th>
            <th style="width:33%">
            </th>
        </tr>
        <tr>
            <td>
                @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin"))
                {
                    <text> Send automatic emails to customers for Initiative Zero.</text>
                }
            </td>
            <td>
                @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin"))
                {
                    <text> Send automatic emails to customers for Initiative Zero Bis.</text>
                }
            </td>
            <td></td>
        </tr>
    </tbody>
</table>

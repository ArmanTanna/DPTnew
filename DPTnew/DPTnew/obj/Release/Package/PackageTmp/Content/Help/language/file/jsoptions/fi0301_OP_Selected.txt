fi0301_OP_Selected="Imports only some selected entities. Clicking the OK button opens the source document in a separate window, enabling you to select the entities to import into the target document. End selection and confirm it by clicking Yes."
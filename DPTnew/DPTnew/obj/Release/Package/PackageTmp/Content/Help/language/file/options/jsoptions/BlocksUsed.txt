BlocksUsed="When selected, only the blocks actually used in the drawing being imported are converted. Blocks defined but not used are ignored and the resulting drawing may be smaller in size. This options is enabled when converting blocks as shared groups."
OP_thread_extent="Choose how far you want the thread to extend: All - applies the thread to the entire selected surface; Height - applies the thread from a starting face to the length specified in the Height box; Edge - applies the thread so that it extends between two selected edges"